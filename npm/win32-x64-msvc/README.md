# @globlin/win32-x64-msvc

This is the **Windows x64** binary for [globlin](https://www.npmjs.com/package/globlin).

This package is automatically installed when you install `globlin` on a Windows x64 system.

**Do not install this package directly** - install `globlin` instead.

```bash
npm install globlin
```
